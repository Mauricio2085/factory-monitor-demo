/**
 * Placeholder seed runner so `npm run db:seed` resolves after build.
 * Sample data lives in schema.sql; add versioned seeds here if needed.
 */
console.log("No additional seeds to run.");
process.exit(0);
